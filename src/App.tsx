import Button from '@components/Button';
import { Auth } from '@components/auth/auth';

function App() {
  return (
    <h1>
      HolaX
      <Auth />
      <Button />
    </h1>
  );
}

export default App;
